# 이 블로그의 규칙

게임과 서브컬처의 역사를 정리하는 개인 아카이브입니다. Jekyll로 만들고 GitHub Pages에서 빌드합니다.

이 문서는 **새 글을 쓰거나 고칠 때 지켜야 할 것**을 모아둔 것입니다. Claude Code는 이 파일을 자동으로 읽습니다. 다른 도구로 초고를 쓸 때는 이 파일을 통째로 붙여넣고 시작하세요.

---

## 1. 저장소

```bash
bundle install
bundle exec jekyll serve   # http://localhost:4000
```

글은 `_posts/YYYY-MM-DD-slug.md`에 둡니다. 파일명의 날짜가 URL이 되고, 슬러그는 영문 소문자와 하이픈만 씁니다.

---

## 2. Front matter

다섯 줄이 전부 필요합니다. 하나라도 빠뜨리지 마세요.

```yaml
---
title: "코에이 『삼국지』 3편 — 46개 도시, 우리가 아는 삼국지의 형태가 완성되다"
description: "46개 도시와 직책, 야전·공성전·수전으로 코에이 삼국지의 형태를 완성한 1992년 『삼국지 III』. 일본 판례와 전자신문 기사까지 대조했다."
category: 게임역사
tags: [삼국지, 코에이, 역사시뮬레이션, 레트로게임, PC9801]
date: 2026-08-27 11:54:00 +0900
---
```

- **description** — 한두 문장. 홈 화면 카드와 검색 결과에 그대로 나옵니다. 없으면 사이트 전체 설명이 대신 쓰이므로 반드시 넣습니다.
- **category** — 아래 다섯 중 하나만. 새 카테고리를 쓰려면 `_config.yml`의 `category_list`에도 추가해야 합니다.
  `게임역사` / `게임뉴스` / `캐릭터평전` / `인물` / `서브컬처`
- **date** — 시간까지 적고 `+0900`을 붙입니다.

---

## 3. 문장

이 블로그에서 가장 자주 어긋나는 부분입니다.

### 문단은 2~3문장으로 묶는다

한 문장씩 끊어 쓰면 화면에 들어가는 정보가 줄고 강조가 평준화됩니다. **본문 문단 평균 190~270자**를 기준으로 삼으세요.

```
✗  전투 시작 때 한 번 발생하는 이벤트에 가까웠던 일기토가, III에서는 하나의 명령으로 자리 잡았다.

   장수 둘이 직접 맞붙는 애니메이션과 대사가 들어간다.

   강한 장수가 약한 장수를 단숨에 베어버리기도 했다.

✓  전투 시작 때 한 번 발생하는 이벤트에 가까웠던 일기토가, III에서는 **전투 중에 사용할 수 있는
   하나의 명령**으로 자리 잡았다. 장수 둘이 직접 맞붙는 애니메이션과 대사가 들어가면서 무력이라는
   숫자가 화면 위의 장면으로 변한다. 강한 장수가 약한 장수를 단숨에 베어버리거나, 예상 밖의 필살
   연출이 나오기도 했다.
```

한 줄짜리 문단은 **정말 세게 박고 싶은 한 문장에만** 씁니다. 섹션당 한 번을 넘기지 마세요.

### 볼드는 섹션당 하나

`**강조**`는 그 섹션이 주장하는 것 하나, 또는 논지를 지탱하는 숫자 하나에만 씁니다. 긴 글에서 볼드가 스무 개를 넘어가면 독자는 더 이상 그것을 강조로 읽지 않습니다.

쓰지 않는 곳:

- 소제목이 이미 말한 작품명 (`### Fable — …` 밑에서 다시 `**Fable**`)
- 고유명사, 사람 이름, 회사명
- 목록 앞머리의 연도 (`- **1997** 마리의 아틀리에` → `- 1997 마리의 아틀리에`)
- 이미 따옴표가 감싼 인용
- 표에서 한 열 전체 (그건 강조가 아니라 그냥 데이터입니다)

### 도입부는 질문으로 연다

자료 목록을 나열하며 시작하지 마세요. 무엇이 이상해서 찾아보게 됐는지를 먼저 씁니다.

```
✗  이번 글에서는 코에이테크모·거스트의 공식 자료와 일본 개발자 인터뷰를 중심으로,
   1997년 마리부터 2027년 카리아까지 아틀리에 시리즈가 어떻게 변해왔는지를 정리한다.

✓  궁금해진 건 이 지점이다. 이렇게까지 달라졌는데 왜 전부 아틀리에라고 부를 수 있는 걸까.
   거스트가 30년 동안 계속 바꾼 것과 끝까지 바꾸지 않은 것은 각각 무엇일까.

   코에이테크모·거스트의 공식 연표와 일본 개발자 인터뷰를 따라가며 정리해봤다.
```

무엇을 근거로 썼는지는 도입부 **마지막에 한 줄로** 붙입니다. 뉴스·프리뷰 글은 예외로, 기준 시점을 명확히 밝히는 문장을 넣으세요 (`이 글은 2026년 8월 27일 기준으로…`).

### 글은 문장으로 끝낸다

표나 목록으로 끝나면 글이 결론 없이 멈춘 것처럼 보입니다. 연표나 정리 표는 **결론 앞에** 두고, 마지막 섹션은 판단으로 닫습니다.

마무리 제목은 글마다 다르게 씁니다. `정리하며`, `마무리`, `지금 다시 보면`을 여러 글에 반복하면 형식이 눈에 띕니다. 시리즈 안에서 일부러 통일하는 경우는 예외입니다 (삼국지 1·2·3편이 그렇습니다).

### 화자를 남긴다

About에 "자료를 찾아가며 함께 알아가는 과정 자체도 이곳의 일부"라고 써 두었습니다. 문체도 거기에 맞춥니다.

- `…라고 생각한다`, `…인 것 같다`, `…가 궁금해서 찾아봤다`, `정리해봤다`
- 확인하지 못한 것은 확인하지 못했다고 씁니다. 출처마다 다른 정보는 단정하지 않습니다.
- 긴 글이라면 이런 문장이 서너 개는 있어야 합니다.

```
다만 이것은 코에이 개발자가 직접 밝힌 공식 개발 비화가 아니라 일본 팬 연구에 기반한
해석이므로 그대로 단정할 수는 없다. 그래도 함께 놓고 보면 꽤 흥미로운 단서다.
```

---

## 4. 표기

**일본 고유명사는 일본어 음으로 적습니다.** 한자를 한국 음으로 읽지 마세요. 과거에 세 번 나온 실수입니다.

| ✗ | ✓ |
|---|---|
| 광영 | 코에이(光栄) |
| 향곡실 | 무카이야 미노루(向谷実) |
| 후지쓰 | 후지쯔 |

한자 병기는 처음 나올 때 한 번만 합니다. 세가나 코에이의 공식 기록을 인용하며 원문 표기를 보일 때는 `코에이(光栄) 발매`처럼 병기해도 됩니다.

작품명은 백틱으로 감쌉니다 — `` `삼국지 III` ``. 제목 줄에서는 『』를 씁니다. 《》나 따옴표는 쓰지 않습니다.

---

## 5. 인용과 출처

본문에서는 **문장으로 출처를 밝히고**, 링크는 글 끝에 모읍니다.

```
✓  1995년 도쿄지방법원 판결은 코에이의 청구를 기각했다. 재판부는 능력치를 1~100으로
   제한하는 것 자체는 프로그램의 표현이라기보다 **사양 또는 아이디어 영역**에 가깝다고
   판단했다.

✗  본문 중간에 링크만 있는 문단이 떠 있는 형태
✗  맨 URL (클릭도 안 되고 모바일에서 화면 밖으로 넘칩니다)
```

글 끝은 이 형식입니다.

```markdown
## 참고 자료

- [코에이테크모 — 『삼국지』 40주년 HISTORY](https://www.gamecity.ne.jp/sangokushi40th/jp/history/)
- [전자신문 — 게임 베스트셀러(1996)](https://www.etnews.com/199606070059)

**작성 기준: 2026-08-27 KST**
```

- 제목은 `## 참고 자료` (h2). 필요하면 `## 참고 자료 — 일본` 처럼 뒤에 붙입니다.
- 링크는 전부 마크다운 링크로. 맨 URL은 두지 않습니다.
- `작성 기준`은 모든 글에 넣습니다.

---

## 6. 링크와 시리즈

**같은 주제를 다룬 글이 이미 있으면 본문에서 링크합니다.** 특히 비교하는 대목에서 링크가 없으면 안 됩니다.

```markdown
[신데렐라걸즈](/2026/08/25/idolmaster-cinderella-girls-origin/)는 100명이 넘는
아이돌로 시작했고, [밀리언 라이브](/2026/08/27/idolmaster-million-live-history/)는
765프로를 50명으로 늘렸다.
```

경로는 `/YYYY/MM/DD/slug/` 형식입니다.

시리즈물은 앞뒤로 서로 링크하고, `series.md`의 목록에 한 줄 추가합니다.

```liquid
{% assign series_defs = "…,코에이 대항해시대|대항해시대,…" | split: "," %}
```

`표시 이름|모을 태그` 쌍을 넣고 해당 글에 그 태그를 붙이면 시리즈 페이지에 자동으로 모입니다.

---

## 7. 태그

태그는 **글을 모으는 도구**입니다. 모으지 못하는 태그는 넣지 않습니다.

- 카테고리와 겹치는 태그 금지 — `게임역사`, `게임뉴스`, `서브컬처`
- 거의 모든 글에 붙는 태그 금지 — `게임`, `트렌드`
- 한 대상은 한 표기로. 한글 표기가 있으면 한글을 씁니다.
  `게임스컴`(○) / `gamescom`(✗), `도쿄게임쇼`(○) / `TGS`(✗), `아틀리에`(○) / `Atelier`(✗)
- 한 글에 3~6개면 충분합니다.

플랫폼 태그는 기존 표기를 따릅니다 — `PC8801`, `PC9801`, `Windows`, `PlayStation`, `FM7`, `X1`.

---

## 8. 마크다운과 구조

- **본문은 `##`부터 시작합니다.** 글 제목이 이미 페이지의 `<h1>`이므로 본문에 `#`을 쓰면 h1이 두 개가 됩니다.
- `##` 위에 `---`를 넣지 마세요. h2에 이미 구분선 스타일이 붙어 있어 두 줄로 겹쳐 보입니다.
- 표는 자유롭게 써도 됩니다. 좁은 화면에서는 표만 가로로 스크롤됩니다 (`post-enhance.js`가 처리).
- `##`가 8개 이상이면 목차가 자동으로 생깁니다. 직접 만들지 마세요.
- 제목에 `A·B·C`처럼 공백 없이 원어를 이어 붙이는 것은 피하는 편이 좋습니다.

---

## 9. 발행 전 점검

```bash
bundle exec jekyll build
```

빌드가 통과한 뒤 아래를 확인합니다.

```bash
f=_posts/2026-08-29-my-post.md

grep -c '^# ' "$f"                              # 0이어야 함
grep -o '\*\*[^*]*\*\*' "$f" | wc -l            # 긴 글이라도 30 안팎
grep -c '작성 기준' "$f"                         # 1 이상
grep -o 'https\?://[^ )]*' "$f" | wc -l         # 아래 값과 같아야 함
grep -o '](https\?://[^)]*)' "$f" | wc -l       # 맨 URL이 없다는 뜻

# 본문 문단 평균 길이 — 190~270 사이
awk 'NR>8' "$f" | grep -v '^$' | grep -v '^#' | grep -v '^[-*|>]' > /tmp/p
echo $(( $(wc -m < /tmp/p) / $(wc -l < /tmp/p) ))
```

그리고 눈으로 볼 것:

- [ ] 도입부가 질문으로 열리는가
- [ ] 마지막 섹션이 표나 목록이 아니라 문장으로 끝나는가
- [ ] 마무리 제목이 다른 글과 겹치지 않는가
- [ ] 판단을 드러낸 문장이 서너 개 있는가
- [ ] 언급한 다른 글에 링크를 걸었는가
- [ ] 일본 인명·회사명을 일본어 음으로 적었는가
